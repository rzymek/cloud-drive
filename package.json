{
  "name": "cloud-drive",
  "description": "Connect to Google Drive from Cloud Function",
  "scripts": {
    "deploy": "gcloud beta functions deploy drive --trigger-http"
  },
  "dependencies": {
    "googleapis": "25.*"
  },
  "private": true
}